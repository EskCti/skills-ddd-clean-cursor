interface:
  display_name: 'Core Entity (C#)'
  short_description: 'Criar entidades de domínio em C#'
  default_prompt: 'Crie ou revise uma entidade de domínio em C# seguindo o padrão do projeto (PascalCase, record/class, Result), com invariantes, validação e testes.'
