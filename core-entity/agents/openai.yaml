interface:
  display_name: 'Core Entity'
  short_description: 'Criar entidades no padrão Genérico'
  default_prompt: 'Crie ou revise uma entidade de domínio seguindo o padrão do projeto (Entity/Result), com invariantes, validação e testes.'
