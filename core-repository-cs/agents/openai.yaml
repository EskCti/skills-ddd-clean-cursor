interface:
  display_name: 'Core Repository (C#)'
  short_description: 'Criar interfaces e implementações de repositório em C#'
  default_prompt: 'Crie ou revise um contrato (interface) ou implementação de repositório em C# seguindo o padrão do projeto (PascalCase, Result, async/await).'
