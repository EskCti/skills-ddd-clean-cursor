interface:
  display_name: 'Config Shared Core (Kotlin)'
  short_description: 'Inicializar kernel compartilhado de domínio em Kotlin'
  default_prompt: 'Crie ou recrie o módulo packages/shared em Kotlin com classe base Entity, VOs (Id, Name, Email, HashPassword), interface UseCase, PagedResult (com PaginationMeta), TransactionManager e testes unitários completos.'
