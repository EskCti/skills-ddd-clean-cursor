interface:
  display_name: "Config Module"
  short_description: "Criar módulo full-stack (package + backend + frontend)"
  default_prompt: "Crie um novo módulo com node .agents/skills/config-module/scripts/create-module.mjs gerando package, backend e frontend com nomenclatura kebab-case, e sincronize a dependência <scope>/<modulo> em apps/backend/package.json e apps/web/package.json."
